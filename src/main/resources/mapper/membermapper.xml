<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.admin.mapper.MemberMapper">
<!-- 회원정보 저장 -->
<insert id="insertUser" parameterType="ReqMemberInsert"	useGeneratedKeys="true" keyColumn="member_sid" keyProperty="member_sid">
	INSERT	INTO	tb_member(
		email,
		password,
		nick_name,
		delete_yn,
		created_date,
		modified_date
	) VALUES (
		#{email},
		#{password},
		#{nick_name},
		'N',
		now(),
		now()
	)
</insert>

<!-- 이메일 중복체크 -->
<select id="emailChk" 	parameterType="ReqMemberInsert" resultType="ResEmailNameChk">
	SELECT email
	FROM 	tb_member
	WHERE	
		email = #{email}
</select>
<!-- 회원명 중복체크 -->
<select id="nameChk"		parameterType="ReqMemberInsert" resultType="ResEmailNameChk">
	SELECT nick_name
	FROM 	tb_member
	WHERE	
		nick_name = #{nick_name}
</select>
<!-- 로그인 -->
<select id="loginUser" 		parameterType="ReqMemberInsert" resultType="ResLoginUser">
	SELECT member_sid,email,password
	FROM	 tb_member
	WHERE
				email = #{email}
	AND		password = #{password}
</select>
</mapper>